let elements = document.querySelectorAll("*");
for (let i = 0; i < elements.length; i++) {
    elements[i].style.setProperty("background-color", "#1e1e1e", "important");
    elements[i].style.setProperty("color", "#f2f2f2", "important");
}
